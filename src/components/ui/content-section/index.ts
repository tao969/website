/**
 * content-section — Scroll-reveal text + scramble block
 *
 * Reusable on any page that needs a numbered section with paragraphs.
 *
 * Usage:
 *   import { ContentSection } from '@/components/ui/content-section'
 */

export { ContentSection } from './content-section';
